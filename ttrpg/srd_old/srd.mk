# srd.mk (same directory as Makefile)
NAME = Fate's Edge - jSRD.pdf
FILE = fates-edge-srd-main.tex
# Optional if auto-detection fails:
#GIT_ROOT = 
# Or point directly to your script:
SCRIPT = "$GIT_ROOT/tools/compile_latex.sh"
